{
  "name": "steeleye",
  "version": "1.0.0",
  "description": "STEELEYE - Advanced project management system with intelligent blueprint analysis and cost estimation. Frontend-only system that uploads PDFs and Excel files, detects pieces, counts and marks matches by page with color highlights, shows summary and filters, and allows CSV/JSON export.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.1"
  },
  "dependencies": {
    "lucide": "^0.545.0"
  }
}
